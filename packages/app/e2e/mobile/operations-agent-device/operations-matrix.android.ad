context platform=android timeout=600000 retries=0
env APP_ID=sh.paseo.debug

open "${APP_ID}"
settings appearance light
wait "id=\"menu-button\"" 45000

press "id=\"menu-button\""
wait "id=\"sidebar-settings\"" 30000
press "id=\"sidebar-settings\""
wait "id=\"settings-host-picker\"" 30000
press "id=\"settings-host-picker\""
wait "id=\"settings-add-host\"" 30000
press "id=\"settings-add-host\""
wait "id=\"add-host-method-direct\"" 30000
press "id=\"add-host-method-direct\""
wait "id=\"direct-host-input\"" 30000
fill "id=\"direct-host-input\"" "${SECONDARY_HOST}"
fill "id=\"direct-port-input\"" "${SECONDARY_PORT}"
keyboard dismiss
press "id=\"direct-host-submit\""
wait "id=\"host-page-connections-card\"" 30000

back
wait "id=\"settings-host-picker\"" 30000
back
wait "id=\"menu-button\"" 30000
press "id=\"menu-button\""
wait "id=\"sidebar-operations\"" 30000
press "id=\"sidebar-operations\""
wait "id=\"operations-screen\"" 30000
wait "id=\"operations-summary-working\"" 30000
wait "id=\"operations-summary-attention\"" 30000
wait "id=\"operations-summary-idle\"" 30000
wait "id=\"operations-workspace-${PRIMARY_SERVER_ID}-${PRIMARY_WORKSPACE_ID}\"" 30000
screenshot "${AD_ARTIFACTS}/operations-light.png" --scale 1

orientation landscape-left
wait "id=\"operations-screen\"" 30000
wait "id=\"operations-summary-working\"" 30000
screenshot "${AD_ARTIFACTS}/operations-landscape.png" --scale 1
orientation portrait
wait "id=\"operations-screen\"" 30000

home
open "${APP_ID}"
wait "id=\"operations-screen\"" 30000
wait "id=\"operations-summary-attention\"" 30000
screenshot "${AD_ARTIFACTS}/operations-resumed.png" --scale 1

press "id=\"operations-workspace-${PRIMARY_SERVER_ID}-${PRIMARY_WORKSPACE_ID}\""
wait "id=\"workspace-header-title\"" 30000
press "id=\"menu-button\""
wait "id=\"sidebar-operations\"" 30000
press "id=\"sidebar-operations\""
wait "id=\"operations-screen\"" 30000

press "id=\"operations-agent-${PRIMARY_SERVER_ID}-${PRIMARY_AGENT_ID}\""
wait "id=\"message-input-root\"" 30000
press "id=\"menu-button\""
wait "id=\"sidebar-operations\"" 30000
press "id=\"sidebar-operations\""
wait "id=\"operations-screen\"" 30000

scroll down 0.7
scroll down 0.7
wait "id=\"operations-provider-subagent-${PRIMARY_SERVER_ID}-${PRIMARY_AGENT_ID}-${PRIMARY_PROVIDER_SUBAGENT_ID}\"" 30000
screenshot "${AD_ARTIFACTS}/operations-primary-hierarchy.png" --scale 1

scroll down 0.8
scroll down 0.8
scroll down 0.8
wait "id=\"operations-workspace-${SECONDARY_SERVER_ID}-${SECONDARY_WORKSPACE_ID}\"" 30000
wait "id=\"operations-agent-${SECONDARY_SERVER_ID}-${SECONDARY_AGENT_ID}\"" 30000
wait "id=\"operations-provider-subagent-${SECONDARY_SERVER_ID}-${SECONDARY_AGENT_ID}-${SECONDARY_PROVIDER_SUBAGENT_ID}\"" 30000
settings appearance dark
wait "id=\"operations-screen\"" 30000
screenshot "${AD_ARTIFACTS}/operations-dark-hierarchy.png" --scale 1

press "id=\"operations-agent-${SECONDARY_SERVER_ID}-${SECONDARY_AGENT_ID}\""
wait "id=\"message-input-root\"" 30000
close
